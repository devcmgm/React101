import React from 'react';
import App from './App';
import { shallow } from 'enzyme';

describe('Test APP', () => {
  it('Renders', () => {
    //const wrapper = shallow(<App/>);
    expect(true);
  });
})
